Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
drf-spectacular==0.28.0
django-filter==24.3
django-cors-headers==4.6.0
psycopg[binary]==3.2.3
dj-database-url==2.3.0
whitenoise==6.8.2
celery==5.4.0
redis==5.2.1
django-storages==1.14.4
boto3==1.35.80
python-dotenv==1.0.1
# E.164 phone normalisation. Contact de-duplication (SRS 3.1.3 / 3.1.10) matches on the
# normalised number, so "050 123 4567" and "+971501234567" have to collapse to one key —
# which is a per-country rule set, not a regex.
phonenumbers==9.0.39
gunicorn==23.0.0
# Explicit pin: already installed transitively via botocore, but rent-schedule generation
# and task recurrence depend on it directly — an implicit dependency is a future breakage.
python-dateutil==2.9.0.post0
